#include <iostream>
#include <vector>

using namespace std;

vector<vector<int>> v(101, vector<int>(-1));

int main(){
    int n, m;
    for (int i = 0; i <= n; i++){
        for (int j = 0; j <= m; j++){

        }
    }
}